def hint_username(username):
    if len(username) < 3:
        print("Invalid username. Must be at least 3 Chars long")
    elif len(username) > 15:
        print("Invalid username. Must be at least 15 chars long")
    else:
        print("Valid username")
